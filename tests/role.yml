---
- hosts: all

  roles:
    - role_under_test

  pre_tasks:
    - name: install environment
      package:
        name: "{{ item }}"
        state: present
      with_items:
        - apache2
        - libapache2-mod-php
